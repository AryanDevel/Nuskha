# Keep the store strict: a package may only import what it declares.
# Catching a missing dependency here is cheaper than catching it in a consumer's build.
hoist=false
strict-peer-dependencies=true
provenance=true
