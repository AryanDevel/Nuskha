## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Unit

<!-- e.g. 0.2, or "none" for a fix outside the phase plan. -->

## How it was verified

<!-- The check, not the intention. Validator output, golden diff, test names. -->

## Checklist

- [ ] `pnpm lint && pnpm typecheck && pnpm build && pnpm test` passes locally
- [ ] Commits are signed off (`git commit -s`)
- [ ] Changeset added if a published package changed (`pnpm changeset`)
- [ ] No real clinical documents added anywhere in the diff
- [ ] ADR added or updated if this constrains future work
