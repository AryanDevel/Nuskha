/**
 * The same surface as `@nuskha/ig`, generated from the ABDM IG 7.0.0 build
 * on the NRCeS preview site. That build is marked `notForPublication` and can
 * change without a version bump, so nothing in Nuskha depends on it. It is
 * here so the move to 7.0.0 can be tried before it is released. See
 * `src/generated/diff-ndhm-6.5.0-to-ndhm-7.0.0-preview.md` for what changes.
 */
export * from "./generated/ndhm-7.0.0-preview/index.ts";
export * as datatypes from "./generated/r4/datatypes.ts";
export type { ProfileEntry, UncheckedConstraint } from "./registry-types.ts";
